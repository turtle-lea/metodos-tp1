#include "../include/metodos.h"
#include <iostream>

using namespace std;

float Metodos::Biseccion(){
	float positivo = (this->functions).semillas_biseccion_f().first;
	float negativo = (this->functions).semillas_biseccion_f().second;
	//Completar el resto del método
}

